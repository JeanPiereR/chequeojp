import 'package:flutter/material.dart';

class PreusoScreen extends StatelessWidget {
  const PreusoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Lista de Preuso")),
      body: const Placeholder(),
    );
  }
}
