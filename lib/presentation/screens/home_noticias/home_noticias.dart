import 'package:flutter/material.dart';

class HomeNoticias extends StatelessWidget {
  const HomeNoticias({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
